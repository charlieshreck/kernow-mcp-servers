---
# A2A API token for REST bridge authentication
# Used by all domain MCPs to validate incoming requests from A2A orchestrator
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: a2a-api-token
  namespace: ai-platform
spec:
  hostAPI: https://app.infisical.com/api
  authentication:
    universalAuth:
      credentialsRef:
        secretName: universal-auth-credentials
        secretNamespace: infisical-operator-system
      secretsScope:
        projectSlug: prod-homelab-y-nij
        envSlug: prod
        secretsPath: /platform/a2a
  managedSecretReference:
    secretName: a2a-api-token
    secretNamespace: ai-platform
    secretType: Opaque
    creationPolicy: Owner
